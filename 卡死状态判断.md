# 卡死状态判断（opencode 插件侧）

> 针对问题：opencode 接收大模型回复时可能卡死 20 分钟以上（界面停在 thinking/processing，模型已无输出），插件能否检测卡死状态并触发中断、恢复输入提示词？

## 1. 结论（TL;DR）

- opencode **没有**「卡死状态」专用插件 API。插件能拿到的只有：
  - 生命周期事件：`session.status`（`busy` / `retry` / `idle`）、`session.idle`（deprecated）、`session.error`
  - 进度心跳事件：`message.part.delta`、`message.part.updated`、`message.updated`
  - RPC：`client.session.prompt(...)`、`client.session.abort(...)`、`client.session.status()`
- 卡死的可判定定义：**会话状态仍为 `busy`，但长时间没有任何进度事件**。所以检测方案是**心跳超时**。
- 中断/恢复方案：`client.session.abort({ path: { id }, query: { directory } })` → 会话被中断，`session.status` 立即回到 `idle`，随后插件可用 `client.session.prompt(...)` 重新发送提示词。
- 前提确认：opencode v1 会话循环**没有**对 LLM 流的空闲超时（`packages/opencode/src/session/prompt.ts`、`processor.ts` 中无 `Effect.timeout` 包裹流）。20+ 分钟无输出时，唯一能自动恢复的路径就是插件层 `abort`。

## 2. 可用 API / 事件盘点（对照源码）

| 能力 | 形式 | 源码位置 |
|---|---|---|
| 发送提示词（阻塞到回复完成） | `client.session.prompt({ path: { id }, body: { parts: [{ type: 'text', text }] } })` → `POST /session/{id}/message` | `packages/sdk/js/src/gen/sdk.gen.ts`；`types.gen.ts` `SessionPromptData` |
| 中断会话 | `client.session.abort({ path: { id }, query?: { directory } })` → `POST /session/{id}/abort` → boolean | `server/routes/instance/httpapi/handlers/session.ts:232`；`groups/session.ts:91` |
| 查询状态 | `client.session.status()` → `GET /session/status` | `SessionStatus.list`，`session/status.ts:35` |
| 状态事件 | `session.status`：`{ sessionID, status }`，`status.type ∈ {busy, retry, idle}`；`retry = { type:'retry', attempt, message?, action?, next }` | `schema/src/session-status-event.ts:11-30`；发布点 `session/status.ts:39-47` |
| 空闲事件 | `session.idle`：`{ sessionID }`（deprecated 但仍发布） | `session/status.ts:42-43` |
| 进度心跳 | `message.updated`、`message.part.updated`、`message.part.delta` | `session/session.ts:633,639,886` |
| 权限/提问 | `permission.asked/replied`、`question.asked/replied/rejected` | 经 EventV2Bridge 转发 |

### 中断链路（abort 之后发生了什么）

1. `SessionPrompt.cancel(sessionID)` → `state.cancel(sessionID)`（`session/prompt.ts:152-154`）
2. `SessionRunState.cancel`（`session/run-state.ts:77-85`）：
   - 先取消该会话的 background jobs（`cancelBackgroundJobs`）
   - `status.set(sessionID, { type: "idle" })` → **立即发布 `session.status(idle)` + `session.idle` 事件**
   - 再中断运行 fiber（`existing.cancel`）
3. 被中断的正在生成消息由 `finalizeInterruptedAssistant` 收尾：`error.name === "AbortedError"`（`message-v2.ts:611-617`，`DOMException("Aborted","AbortError")` 映射为 `AbortedError`），并补 `time.completed`（`prompt.ts:1203-1211`）。
4. 会话回到 idle → 输入可用 → 插件可重新 `client.session.prompt(...)`。

## 3. 检测方案（推荐实现）

1. 对每个会话维护 `lastActivityAt`。
2. `event` hook 中：
   - `session.status` 为 `busy` 时：开始监听（记录 `busySince`，`lastActivityAt = now`）。
   - 收到进度事件 `message.part.delta` / `message.part.updated` / `message.updated`：刷新 `lastActivityAt = now`。
   - `session.status` 为 `idle`：取消卡死计时，复位。
3. 定时器/轮询：`status === 'busy' && now - lastActivityAt >= threshold` → 判定卡死。阈值建议可配置（如 `stuck_threshold_minutes`，默认 10–15 分钟）。
4. 兜底：定期 `client.session.status()` 轮询，确认仍是 `busy`（防止事件丢失导致误判）。
5. 防误判：
   - 工具长执行期间也会有 part 心跳（tool part state 变化、输出增量），推理期间有 reasoning delta —— 因此用「零心跳时长」而非「busy 总时长」。
   - `retry` 状态单独处理（会带 `attempt/next` 重试信息），retry 期间不算 busy、也不要立即发提示词。
   - 等待 `question` / `permission` 时也处于 busy 但**不应**判卡死：跟踪 `waitingQuestion` / `waitingPermission` 并豁免（或缩短阈值）。

## 4. 中断与恢复时序

```
判定卡死
  → client.session.abort({ path: { id }, query: { directory } })   // directory 需匹配会话工作目录
  → 服务端取消 background jobs → status.set(idle) 发布 session.status(idle) + session.idle
  → run fiber 中断，assistant 消息标记 AbortedError
插件收到 session.status(idle)
  → （可按需延迟）client.session.prompt({ path: { id }, body: { parts: [{ type:'text', text: 恢复提示词 }] } })
  → prompt 再次阻塞到新回复完成 → 状态回到 idle
```

## 5. 注意事项 / 坑

- `abort` 幂等：对已 idle/已结束的会话 abort 是 no-op，可放心调用。
- v1 / v2 差异：
  - 本 dev 仓库的 V2 HTTP API 中断端点是 `session.interrupt`（`POST /api/session/:sessionID/interrupt`），事件为 `session.next.*`（`permission.v2.*` / `question.v2.*`）。
  - 已发布 CLI（v1.x）走 v1 client：`session.abort` + `session.status` / `message.part.*`。插件基于 `@opencode-ai/plugin ^1.18.x` 用 v1 形态即正确。
- `abort` 之后不要把 `AbortedError` 当成业务错误重试；并发发送时若会话仍 busy，`prompt` 会抛 `Session.BusyError`，发送前先确认 idle。
- 服务端无空闲超时，卡死只能插件兜底。

## 6. 与现有插件 `opencode-idle-continue` 的关联

- 现有检测器只对 `idle` 生效，对「busy 且零心跳」的卡死**完全无感知** —— 需要新增上述心跳超时逻辑。
- 插件监听的 `question.replied2` / `question.rejected2` 事件名不存在，正确名称为 `question.replied` / `question.rejected`（会导致 `#waitingQuestion` 永不复位、idle 检测被永久抑制）。
- 中断后消息 `error.name` 是 `AbortedError`，不是插件当前判定的 `MessageAbortedError`。



## opencode 其他问题

Key conclusion: opencode has no stuck-state API — detect it as busy + zero progress events (message.part.delta/updated, message.updated) beyond a threshold, then client.session.abort({ path:{id}, query:{directory} }). Abort chain confirmed in-source: SessionPrompt.cancel → SessionRunState.cancel cancels background jobs, status.set(idle) emits session.status+session.idle, then interrupts the run fiber, marking the in-flight message error.name === "AbortedError" (prompt.ts:152-154, run-state.ts:77-85, message-v2.ts:611-617). After idle, re-send via client.session.prompt(...). Also confirmed v1 has no server-side LLM-stream timeout, so plugin-side abort is the only auto-recovery.
Plugin review findings (highest-impact first):
1. question.replied2/question.rejected2 don't exist (opencode-true-idle-detector.js:222-223, also index.js:206). Real names: question.replied/question.rejected. After the first question, #waitingQuestion never resets → the idle check at :180 is permanently blocked → plugin stops forever.
2. Abort detection wrong name: checks MessageAbortedError (:151,:231) but the real value is AbortedError → #onUserInterrupt/#interrupted never fire on abort.
3. No stuck-busy detection at all: only reacts to idle; ignores message.part.* heartbeats and has no staleness timeout — exactly the 20-min hang case (the reason for the report above).
4. session.status retry unhandled (:173-198): retry is neither idle nor busy, so nothing is scheduled and oldStatus==='idle' busy-transition reset (:185) never runs after a retry.
5. Minor: properties.info?.id (:170) is always undefined (session.idle payload is {sessionID}); #interrupted is set but never consumed to block a post-abort resend.

## 7. 用户输入活动检测（基于源码分析）

> 针对需求：在插件中检测用户是否正在输入文字，即使输入最后删除了没有发送，也要能够检测到。

### 源码分析结论

通过对 OpenCode 项目源代码的深入分析（特别是 `packages/tui/src/component/prompt/index.tsx`），确认了以下能力：

**✅ OpenCode 完全具备检测用户正在输入文字的能力**

核心检测机制位于 TUI 组件的实时事件监听系统：

```tsx
// 核心监听机制（packages/tui/src/component/prompt/index.tsx:1377-1383）
onContentChange={() => {
  const value = input.plainText
  setStore("prompt", "input", value)
  auto()?.onInput(value)  // 实时检测输入变化
  syncExtmarksWithPromptParts()
  setCursorVersion((value) => value + 1)
}}
```

**关键特性：**
- **实时检测**：每次用户输入、删除或修改内容都会触发 `onContentChange` 事件
- **不依赖发送**：无论用户是否按回车发送，只要有输入活动就会被检测
- **包含删除操作**：即使用户删除了所有内容，也会记录中间的输入状态
- **字符级别精度**：每个字符的输入和删除都会被检测和记录

### 插件中的检测实现代码片段

以下是基于事件系统的插件检测代码示例：

```javascript
// 在插件中检测用户输入活动
const userInputDetector = {
  lastUserInputTime: null,
  userInputActive: false,
  inputActivityThreshold: 5000, // 5秒无输入视为不活跃
  
  init(plugin) {
    // 监听TUI输入变化事件
    plugin.on('tui.prompt.append', (event) => {
      this.recordUserInput();
    });
    
    // 监听会话状态变化
    plugin.on('session.status', (event) => {
      if (event.status.type === 'idle') {
        // 用户可能正在输入
        this.startInputMonitoring();
      }
    });
    
    // 定期检查用户输入活跃度
    setInterval(() => this.checkInputActivity(), 1000);
  },
  
  recordUserInput() {
    this.lastUserInputTime = Date.now();
    this.userInputActive = true;
    console.log('[InputDetector] 用户输入活动检测');
  },
  
  startInputMonitoring() {
    // 会话进入空闲状态，用户可能正在输入
    console.log('[InputDetector] 开始监控用户输入');
  },
  
  checkInputActivity() {
    if (!this.lastUserInputTime) return;
    
    const timeSinceLastInput = Date.now() - this.lastUserInputTime;
    
    if (timeSinceLastInput > this.inputActivityThreshold && this.userInputActive) {
      console.log('[InputDetector] 用户输入活动结束');
      this.userInputActive = false;
      this.onUserInputEnded();
    }
  },
  
  onUserInputEnded() {
    // 用户输入活动结束的回调
    // 可以在这里触发卡死检测或其他逻辑
    console.log('[InputDetector] 用户输入活动结束，可以开始卡死检测');
  }
};

// 在插件初始化时使用
plugin.register(userInputDetector);
```

### 检测能力总结

**可以检测到的情况：**
1. ✅ 正在输入文字 - 每次键入字符都会被记录
2. ✅ 删除输入内容 - 删除操作也会触发事件
3. ✅ 输入后全部删除 - 虽然最终结果为空，但输入过程中会记录所有中间状态
4. ✅ 未按回车的输入 - 不依赖提交，只关注输入活动本身
5. ✅ 光标移动和编辑 - 通过事件系统检测

**应用场景：**
- 输入活动指示器 - 显示"用户正在输入..."
- 输入历史追踪 - 记录用户的输入模式
- 自动保存草稿 - 防止意外丢失未发送的输入
- 输入分析 - 分析用户行为和输入习惯
- 协作功能 - 在多用户环境中显示实时输入状态

**实现原理：**
OpenCode 使用 Solid.js 的响应式系统和事件驱动架构，通过 `onContentChange`、`onCursorChange` 和 `onKeyDown` 等事件实现高精度的用户输入检测。插件可以通过监听相关事件（如 `tui.prompt.append`）来获取用户输入活动的实时状态。

